server:
  host: 0.0.0.0
  port: 8080
  workers: 4
  timeout: 30

logging:
  level: info
  format: json
  outputs:
    - stdout
    - file: /var/log/app.log

monitoring:
  enabled: true
  metrics_endpoint: /metrics
  interval_seconds: 15
